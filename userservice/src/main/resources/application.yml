server:
  port: 8080

spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:sqlserver://DESKTOP-I1SMM2J\SQL;databaseName=ecommerce;trustServerCertificate=true
    username: sa
    password: sql@123
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

  jpa:
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServer2012Dialect
    show_sql: true
    hibernate:
      ddl-auto: none

  cloud:
      compatibility-verifier:
        enabled: false

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka

jwt:
  secret: vjSuperSecretJwtKeyMustBeLong123456
  expiration: 3600000

